// While Loop version
function printNumbers(a, b) {
    while (a <= b) {
        console.log(a);
        a++;
    }
}

printNumbers(1, 10);

// For Loop version
function printNums(a, b) {
    for (let i = a; i <= b; i++) {
        console.log(i);
    }
}

printNums(1, 10);